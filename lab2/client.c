#include "client.h"

int main(void) {
	
	// connfd -- the main server connection socket
	// servfd -- the socket "accept" server socket
	int connfd, servfd;
	short conn_port, serv_port;

    return EXIT_SUCCESS;
}
